{
  "name": "@x-oidc/idp-server",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter @x-oidc/server-core build:types && tsc --noEmit -p tsconfig.json",
    "start": "NODE_ENV=production node dist/main.js"
  },
  "dependencies": {
    "@x-oidc/server-core": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
