{
  "name": "@gitea-oidc/idp-server",
  "version": "1.0.29",
  "private": true,
  "type": "module",
  "main": "dist/main.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "pnpm --filter gitea-oidc build:types && tsc --noEmit -p tsconfig.json",
    "start": "NODE_ENV=production node dist/main.js"
  },
  "dependencies": {
    "gitea-oidc": "workspace:*"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.0.0"
  },
  "engines": {
    "node": ">=22.0.0"
  }
}
