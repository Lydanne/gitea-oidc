{
  "name": "@x-oidc/fastify",
  "version": "2.0.0",
  "description": "Fastify 5 connector for X OIDC",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "default": "./dist/index.js"
    }
  },
  "files": [
    "dist",
    "README.md",
    "LICENSE"
  ],
  "sideEffects": false,
  "scripts": {
    "clean": "node ./scripts/clean.mjs",
    "prebuild": "pnpm --filter @x-oidc/connector-core build",
    "build": "pnpm clean && tsc -p tsconfig.json",
    "pretypecheck": "pnpm --filter @x-oidc/connector-core build",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "pretest": "pnpm --filter @x-oidc/connector-testkit build",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "engines": {
    "node": ">=20.19.0"
  },
  "license": "MIT",
  "dependencies": {
    "@x-oidc/connector-core": "workspace:*",
    "@x-oidc/node": "workspace:*"
  },
  "peerDependencies": {
    "fastify": "^5.0.0"
  },
  "devDependencies": {
    "@x-oidc/connector-testkit": "workspace:*",
    "@types/node": "^22.0.0",
    "fastify": "^5.10.0",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10"
  }
}
